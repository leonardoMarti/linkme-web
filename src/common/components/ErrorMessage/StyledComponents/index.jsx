import styled from 'styled-components';
import { COLORS } from '../../../utils/colors';

export const Error = styled.div`
  font-size: 14px;
  font-weight: 400;
  color: ${COLORS.RED};
  margin-top: 5px;
`;
