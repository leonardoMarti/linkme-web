export const selectStyles = {
  control: (base) => ({
    ...base,
    minHeight: '50px',
    border: '0.5px solid #c4c4c4',
  }),
  menu: (base) => ({
    ...base,
    zIndex: 100,
  }),
  menuList: (base) => ({
    ...base,
    zIndex: 100,
  }),
};
