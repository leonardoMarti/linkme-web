export const selectStyles = {
  container: (base) => ({
    ...base,
    width: '100%',
  }),
  control: (base) => ({
    ...base,
    minHeight: '50px',
    border: '0.5px solid #a9a9a9',
  }),
  menu: (base) => ({
    ...base,
    zIndex: 100,
  }),
  menuList: (base) => ({
    ...base,
    zIndex: 100,
  }),
};
